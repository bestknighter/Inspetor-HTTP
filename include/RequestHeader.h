#ifndef REQUESTHEADER_H
#define REQUESTHEADER_H

#include <string>

namespace HTTP {

class RequestHeader {
public:
	RequestHeader();
	RequestHeader( std::string header );
private:
};

};

#endif // REQUESTHEADER_H
